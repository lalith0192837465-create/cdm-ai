import Link from "next/link";
export default function RunLocal(){return <main className="guide-shell"><nav className="site-nav"><Link href="/" className="brand"><span className="brand-mark">C</span> CDM</Link><Link href="/onboarding" className="back-link">← Guided installer</Link></nav><section className="guide-card installer"><div className="section-label">LOCAL INSTALLER</div><h1>Run CDM on your computer.</h1><p className="muted">This is the safest way to test the real dashboard. The demo needs no setup; local mode gives you a private development copy.</p><div className="install-step"><span>01</span><div><h3>Install Node.js 18+</h3><p>Download the LTS version from <a href="https://nodejs.org" target="_blank">nodejs.org ↗</a>.</p></div></div><div className="install-step"><span>02</span><div><h3>Download the project</h3><p>Clone the existing <code>cdm-ai</code> repository, then open its folder in Terminal.</p><pre>git clone https://github.com/lalith0192837465-create/cdm-ai.git
cd cdm-ai</pre></div></div><div className="install-step"><span>03</span><div><h3>Install and start</h3><pre>npm install
npx prisma generate
npx prisma db push
npm run dev</pre><p>Open <code>http://localhost:3000</code>. Use <code>/demo</code> for sample data or sign in for the real dashboard.</p></div></div><div className="notice"><strong>Keys are optional for the demo.</strong><span>Copy <code>.env.example</code> to <code>.env</code> only when you want Google login, Zoom listening, AI extraction, Slack, or Stripe features.</span></div><h2 id="byoc">Customer-cloud setup</h2><p className="muted">BYOC deployment is intentionally guided per pilot. The repository includes the AWS, GCP, and Azure questions; we do not provision speculative infrastructure before a customer confirms their environment.</p><Link href="/demo" className="button primary">Open the demo <span>→</span></Link></section></main>}
